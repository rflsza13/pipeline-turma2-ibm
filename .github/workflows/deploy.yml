name: CI_IBM

on: [push]

jobs: 
  build:
    runs-on: ubuntu-20.04

    steps: 
    - name: dados do diretório
      run: ls -la

    - name: validando pacote java
      run: mvn package -Dmaven.test.skip

  test:
    runs-on: ubuntu-20.04
    needs: build
    steps: 
    - name: Roda um teste
      run: mvn test